apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: casino-frontend
spec:
  type: LoadBalancer
  selector:
    app: casino-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080